fragment Me on User {
    id
    username
    email
    cachedLevel
    birthday
    nsfwAllowed: isYearsOld(age: 18)
}

mutation SetBirthdayMutation($birthday: NaiveDate!) {
    setBirthday(birthday: $birthday) {
        ...Me
    }
}
